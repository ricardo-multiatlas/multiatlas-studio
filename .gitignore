# ── Secretos (NUNCA subir a GitHub) ──
.env
.env.*
!.env.example

# Dependencias
node_modules/

# Build / salida (se regenera con `npm run build`)
dist/

# Vercel
.vercel/

# Repos de referencia (proyectos externos con su propia licencia; no forman parte de este repo)
open-design/
ui-ux-pro-max-skill/

# Basura temporal de sincronización
_to_delete/
_stage*/
*.tar.gz

# Sistema
.DS_Store
Thumbs.db
*.log
